We need to undo or rollback changes related to:
$ARGUMENTS

Rules:
- Inspect `git status` and recent diffs.
- Propose a rollback plan before executing.
- Prefer the safest option: revert commit(s) or restore files.
- Do not destroy uncommitted work without confirmation.

Deliver:
- Rollback plan
- Commands to run (and why)
- Confirmation points before each irreversible step
